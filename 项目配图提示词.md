# 项目配图提示词

使用内置 image_gen 生成。图片为功能概念示意。

## 初始提示词

Use case: infographic-diagram
为 VisualAIAgent 桌面视觉记忆智能体制作一张专业项目介绍配图，横向16:9，高分辨率。现代科技产品插画与系统架构图结合，浅色背景，深蓝和青绿色强调色，紫色突出AI Agent。清晰克制精致，留白充足，中文准确易读，统一线性图标、圆角模块、有方向连接线。避免密集文字、复杂电路、科幻机器人。

顶部主标题：“VisualAIAgent”
副标题：“看见桌面，记住事件，按需行动”

左侧25%：真实感的固定桌面场景插画，普通USB摄像头朝向桌面和座位，电脑显示器、杯子、手机、瓶子和坐着的用户。在物品周围绘制少量检测框；桌面下方标记“左 / 中 / 右”。标题“本地视觉感知”。

中间45%为视觉核心：大型紫色强调模块“AI Agent”，副标题“理解需求 · 调用工具 · 基于证据反馈”。周围三个相连模块“视觉记忆”（数据库和事件截图图标）、“业务工具”（工具调用图标）、“持续任务”（清单与时钟图标）。有向连接线表现Agent调用业务工具，通过工具查询视觉记忆和管理持续任务。只有一个Agent，不直接分析持续视频。

右侧30%：两张简洁产品卡片。
卡片一“自然语言查询”，用户：“最后在哪里看到手机？”，结果：“最近一次出现在画面右侧”，附小型桌面截图证据缩略图，角落“功能示意”。
卡片二“情境提醒”，条件“18 点后离座 + 手机仍在右侧”，提醒“离开前记得带上手机”，角落“功能示意”。

底部简洁流程带：“摄像头 → 本地 YOLO / 状态机 → 结构化事实 → 本地条件匹配”。
结构化事实连接视觉记忆；本地条件匹配连接AI Agent，标注“条件满足时唤醒”。用户对话图标连接Agent标注“主动提问”。Agent旁小型云图标“千问大模型”，线标注“必要文字事实”。通向云端仅文字或数据符号，绝不暗示持续上传视频。
最底部：“本地持续感知 · 可追溯视觉记忆 · 受控工具调用 · 条件触发提醒”

技术限制：不展示身份识别、情绪、专注度、饮水量，不展示钥匙或笔记本合盖，不展示机械臂、语音或外部推送，不虚构准确率或性能指标。Agent为查询和任务执行核心，本地视觉提供事实。整体布局精美、层级分明，作为一张完整的项目主视觉图。

## 工具标签修订

修改这张项目配图，保持整体布局、桌面场景、颜色、主标题与卡片风格不变。必须修正以下技术错误：1.右上中部“业务工具”框删除“打开应用、文件操作、系统设置、网页查询”等文字，替换为四行“场景查询”“物品查找”“事件搜索”“任务与通知”，副标题改为“调用受控工具，查询事实与管理任务”。2.底部删除第二个紫色“AI Agent”小框，只保留中央大型AI Agent。底部本地条件匹配的箭头向上连接中央大型AI Agent，标注“条件满足时唤醒”；用户对话图标也连接中央大型AI Agent，标注“主动提问”；千问大模型的必要文字事实连线来自中央大型AI Agent。3.“写入视觉记忆”连线应由底部“结构化事实”模块指向上方“视觉记忆”模块，不要指向持续任务。4.持续任务中的“定时检查”改成“本地匹配”。5.截图中的具体日期时间改成“历史截图 · 功能示意”。6.云图标下方文字明确写“千问大模型”，不要写成千问大棋型。其余内容保留，中文清楚正确。整个画面只能有一个AI Agent框。
