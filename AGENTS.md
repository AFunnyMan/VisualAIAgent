# VisualAIAgent 项目工作约定

## 项目与授权范围

本项目是普通 USB 摄像头驱动的视觉记忆智能体，首版同时面向 Apple 芯片 Mac 和 Windows 11 x64。正式功能必须保留真实模型驱动的 Agent 工具调用，持续视觉处理在本地完成。

当前授权（2026-09-07 用户新指令）为连续实施整个路线图，完成所有不需要用户辅助测试的内容；允许多个开发子 Agent 并行，子 Agent 模型最高为 Sol，简单工作可使用更低成本模型。包括业务代码、隔离依赖安装、官方模型准备、离线验证和重要里程碑提交及正常同步。真实 API 凭据、摄像头场景布置及 Windows 实机条件不足时明确记录待用户验证项，继续独立工作。产品本身仍为单 Agent；不扩大首版范围。

后续用户授权一个阶段时，只完成该阶段；授权连续实施整个计划时，在阶段验收通过后继续推进，不反复索要已有授权。缺少条件时记录阻塞，继续已授权且不依赖阻塞的工作，不擅自扩大产品范围。

## 阅读入口与事实来源

1. 阅读适用的上级指引及本文件。
2. 阅读 [GOALS.md](GOALS.md)、[PLANS.md](PLANS.md) 和 [实际进度](harness/build-log.md)。
3. 阅读当前阶段文件、其链接的架构/测试说明和相关上下文；检查真实代码、Git 状态和运行环境。
4. 恢复任务时，先验证日志中的最后状态，不将旧聊天中的承诺当成已完成证据。

原始计划保存在 [计划基线](docs/project-plan-baseline.md)，后续不覆盖；现行决策保存在 [架构说明](docs/architecture.md)。新用户指令优先于基线与旧计划，影响范围必须记录。

目标归 GOALS；顺序归 PLANS；实施步骤和验收归阶段文件；接口归 architecture；决策依据归阶段 context；实际结果归 build-log；审查发现归 code_review。用链接代替重复维护多份事实。

## 实施约束

- 使用 Python 3.11、OpenCV、YOLO26n ONNX、ONNX Runtime CPU、Supervision、SQLite、OpenAI Agents SDK、Streamlit。
- 原生 Mac/Windows 开发与验收，uv 管理隔离环境及锁定依赖；Docker 不作为首版必需环境。
- 单 Agent、七个明确的业务工具；不得引入任意 SQL、Shell 或代码执行工具。
- 不训练模型，不持续上传视频，不加入语音、学习行为识别、多 Agent、向量数据库或完整录像。
- 摄像头故障、暂停、过期画面和采样不足不得被解释成物品消失。
- 物品识别不是身份识别；同类多个物品展示候选。未知事实明确说明，回答关联真实证据。
- 对话、视觉事实、关注任务分开存储；无 API Key 时标明 Agent 未连接，模拟模型不能替代真实 Agent 验收。
- 依赖优先复用官方稳定接口；锁定实际验证过的版本和模型校验值，保留许可证与来源。

## 验证与记录

先实现可观察的最小完整行为。状态机、幂等、故障恢复等核心逻辑采用有意义的失败用例；简单文档和界面改动不机械添加测试。

执行命令以现场已存在的项目配置为准。[测试说明](docs/testing.md) 中标记“拟建立”的命令在实现前不能当作现有工具，更不能写成通过。

实际测试区分离线、真实 API、真实摄像头与平台验收。模拟通过不代表实机通过；未执行的检查标记未执行并说明原因。记录时间与时区、实际命令、关键结果、限制和证据位置，不把预期结果粘贴成观察结果。

每次里程碑完成或中断前更新实际进度。阶段 context 仅在阶段真正开始时创建，只记影响后续工作的决策与发现。审查问题修复后重新验证；禁止通过删改验收条件掩盖失败。

## Git 与文档维护

用户要求重要步骤提交到仓库。已授权工作内，在可验证里程碑完成后提交；只暂存该里程碑相关文件，提交前检查差异、敏感信息和 Git 状态。当前远程目标为 `https://github.com/AFunnyMan/VisualAIAgent.git`。

不得覆盖、强推、重写未知远程历史或清理用户改动。远程有新提交时先读取和协调。当前文档任务允许正常推送；后续发布、部署或新增外部系统不由本约定自动授权。

不提交 API Key、真实运行数据库、私人摄像头图片、模型权重、虚拟环境或完整运行日志。项目专属经验先更新项目文档，不修改全局 Codex 指引或创建新 Skill。

## 审查重点

检查 Agent 工具是否真正执行、事件是否有证据、等待时是否意外调用模型、提醒是否重复、UI 是否重复启动工作线程、平台支持是否经过验证、日志是否把计划写成结果。
