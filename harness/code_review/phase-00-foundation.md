# 阶段 00 审查

2026-09-07，Asia/Shanghai。审查基础配置、锁文件、默认离线标记、秘密忽略和实例互斥。

- Config 加载不创建 API 客户端；Key 从 repr 排除，三个显式配置才标记 Agent 已配置。
- 数据、模型、环境和工具缓存被忽略；导出依赖仅在锁文件解析，主环境无 torch/ultralytics。
- pytest 默认排除 live_api/camera；测试文件只使用临时路径，不启动设备。
- 进程锁在已有实例时明确失败，释放后可以重获；Windows 分支尚未实机运行，不声称验收。
- 基础 8 项测试通过；依赖五个关键包实际导入通过。现无阻断基础里程碑的问题。

真实模型、摄像头和整体 UI 不属于这次基础通过范围。
