# 文档准备审查

时间：2026-09-07T02:33:18+08:00。范围：当前文档工作树、原始计划提交 `3180005`、忽略规则和阶段索引。仅审查文档，不是业务代码或产品验收。

## 观察与处理

- 原始计划提到本次不自动提交，用户最新请求明确要求 Git 仓库和重要步骤提交。处理：原始正文不变，AGENTS、PLANS 和日志记录新授权及优先关系。
- 用户要求先文档、后续主动开始任务。处理：所有业务阶段标记未开始；阶段 00 的运行环境仍待实施；不生成阶段 context、不安装依赖或访问摄像头。
- 无 API Key、无 Windows 实机证据。处理：作为实际验收条件保留，不以模拟/容器测试替代，也不阻塞独立的后续离线开发。
- Docker 是环境讨论后的补充取舍。处理：与原始基线分离，现行架构使用原生 Mac/Windows，容器化不成为首版附加任务。
- 软件版本、测试命令及性能未实测。处理：文档将其标为待核验或拟建立，未伪造测试通过结果。

## 已执行的文档检查

使用一次性 Python 检查（未保存脚本到项目）遍历 Markdown，检查本地链接、尾随空白、代码围栏配对、七个阶段顺序与必要章节、疑似凭据格式、应用文件和阶段 context 是否被意外创建，并将基线字节与 `git show 3180005:docs/project-plan-baseline.md` 比较。

当时结果：20 个 Markdown、69 个本地链接、7 个阶段计划；错误 0；应用文件 0；阶段 context 0。此审查记录及随后追加的日志在最终提交前再次做同类检查。

基线 SHA-256：`c28566e288f0ec7f3018699e951a8173166f21b03322719e8ae00e9d6c25692c`。

`git check-ignore` 对 data/test.db、models/test.onnx、.env、.venv/test 和 harness/artifacts/private.png 均返回匹配，退出 0。没有创建这些测试路径对应的文件。

## 结论与限制

首次暂存后发现额外问题：`.gitignore` 中未限定根目录的 `build/` 同时忽略了 `harness/build/`，导致七份阶段文档存在于工作树但没有进入第二次提交。`git check-ignore -v harness/build/phase-00-foundation.md` 定位到该规则。

修复：将构建产物忽略项改为根目录的 `/build/`、`/dist/`，保留阶段文档可正常暂存；在后续提交前新增索引完整性检查，要求七份阶段文档与全部 Markdown 均在 Git 索引中。此问题说明文件存在/链接检查不能替代提交清单检查。

原始计划已保留，后续阶段有目标、步骤、验收和恢复说明。没有实现或验证摄像头、ONNX、Agent API、数据库、Streamlit 或双平台性能。最终索引及同步检查结果归日志。

此次检查不验证外部网页当前可达性，不保证尚未安装的依赖兼容性。远程提交/同步的实际结果另见 [build-log](../build-log.md)。
