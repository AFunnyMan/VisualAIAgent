# 固定场景行为试训审查

2026-09-09，Asia/Shanghai。开发及独立审查子Agent均为GPT-5.6 Sol；root复核关键代码、源视频拼图和训练执行。

## 已处理问题

- 普通分类目录检查只能阻止同图跨split，无法阻止同源视频不同帧跨split。普通训练/验证模式现在要求源组manifest、核验每张图SHA及所属split，并拒绝同源组跨train/val。首轮实际仅train，不伪造val。
- 输出目录先检查再创建存在竞态；现原子预留放在异常写清单逻辑之外，失败时不写未知目录。
- 分类器默认随机裁剪可能截掉杯口/面部。全画幅等比填充变换可序列化，训练仅小幅颜色扰动，验证及ONNX输入使用同一几何/RGB/归一化代码。默认翻转、擦除关闭。
- Ultralytics初始化需要val loader但当前无独立验证集。首轮明确train-only、val=False、覆盖validate/final_eval、固定最后一轮导出；训练日志“val images”是占位初始化，不是质量验证。
- ONNX只导出不能视为验证：新增独立评估工具核验SHA、类别元数据、静态FP32输入/输出及概率语义，并在真实执行时要求相同输入PT/ORT分类一致及maxabs≤1e-4。执行结果见训练日志。
- 空座分类与饮水输出可能矛盾；空座/未知人体证据不能确认饮水事件。时间跳变、断流与unknown中断动作连续性。坐着与饮水可同时成立，站立单帧不等于起身。

## 保留限制

- 三段均为训练素材，仅两次饮水、少量短站立片段。所有重放属于开发数据复测，不是独立动作准确率或实机验收。
- 时序仅实验内存状态，未连接正式通知和持久化业务；不把实验边沿去重宣称为产品通知幂等验收。
- 过渡状态可能被置信度门限拒识而漏掉起身；不能为消除漏报把未知直接改成离座或站立。回放须如实记录漏事件与误事件。
- 正式物品模型、环境配置、三类接口和七业务工具未被本轮替换。Windows行为模型实机、摄像头实时行为准确率尚未验证。

## 2026-09-10 r02 评估与隔离审查

开发与独立标注子Agent均Sol。已处理：评估器按显式split只读该组图片/视频；对历史r01重新计算训练数据的路径/内容指纹、计数与类别，再与逐样本来源清单绑定，防止从其他目录恢复出错误的训练源。test与任一模型train或实际val源重叠均拒绝；模型未参与的其他任务/组不冒充其训练来源。纯val/test数据集可独立评估，无需复制train。

样本结果增加每类置信门限下接纳正确、接纳错误和unknown，避免负例多数及argmax掩盖漏检。事件时间为各视频局部时间，人工窗口按同类一对一匹配；缺少expected_events不能当成零真值。重放核对完整解码时长，不把截断当完成。

r01新视频开发验证确认两个问题：饮水真实正例多数分类错；离座过渡单帧unknown丢失稳定前态。本轮先增加开发训练数据，保持原时序故障保护；有界unknown桥接仅为候选方案，因无法从分类器unknown区分遮挡与普通过渡，未直接接入。

测试前冻结评分容差1秒（.5秒采样延迟+.5秒确认），不修改动作真值、不增加实际事件；旧r01摘要保留原.5秒口径，新旧版本差异在报告说明。最终评估同时记录数据、标注、时序代码与评估代码SHA。root修改后定向16项通过；此前完整离线212通过、3项真实API排除。


## 2026-09-10 r03复审

- 修复ROI训练/推理一致性、原尺寸与裁剪后零面积保护；Torch环境15项通过。修复unknown恢复待确认时泄露历史状态、恢复后未持续提供证据仍桥接等问题；断流/逆序/gap不造动作。
- 人体缓存严格匹配源SHA/帧号/有限时间戳/metadata哈希，重复或缺失帧拒绝；标签修订保持源清单并强制理由，人工语义仍须审查，不能自动证明标签没有迎合预测。
- Sol指出粗人体框不能代替完整动作可见性；维持person-proxy实验标记，正式启用不通过。另指出人体存在与empty标签可能冲突：画面其他区域有人仍可离座，不能用全图person简单否决empty；须验证座位区域关系，当前未实现。
- ±1s匹配偏宽，补零容差结果及原尺寸事件复核；当前17/17仅开发窗口匹配，零容差10/17。小拼图曾误判两饮水为整段误报，扩大顺序解码后确认实际动作存在、部分事件偏早，修订为时点问题。饮水确认延长.5s后现有6动作均匹配，仍不证明实际吞咽。
- ROI模型未优于原分类器，不采用；仍缺独立自然负例/真实遮挡/实时性能，不能将本次开发里程碑当生产验收。
