# 场景微调审查（2026-09-09）

范围：训练授权更新、五个离线准备/训练/评估脚本、训练日志与脱敏评估；没有修改生产视觉、Agent、UI或.env。

## 已修复的实质发现

- 训练器会对已有运行目录自动追加数字：在原子创建独占目录后使用exist_ok=True并核验实际save_dir，禁止覆盖其他实验。
- 小数标签边界的舍入误差触发预检查：加入1e-8数值容差，保留真实越界/NaN失败用例，未删图或改变真值。
- 数据集配置下载指令、错误类别表、图片列表遗漏标签指纹：训练前拒绝远程下载指令，精确核验类别，标签参与SHA与语义校验。
- 同图/同采集组跨分割及测试清单计数：构建阶段拦截，test计数独立计算；现场手工标注要求group，root复核纠正测试手机框。
- PyTorch默认推理分支与nms=False导出分支不同：依据当前官方源码显式选择one-to-one参考，124个输入逐一核验三业务检测输出；首次失败日志保留。
- 训练结束后的最终验证回调被当成额外epoch：实际轮数按results.csv与训练器结束日志确认，后续记录显式区分phase，原始记录不改写。

## 验证与结论

完整离线回归174 passed / 3 deselected；最终训练/评估/分割定向17 passed，Ruff及格式检查通过。两轮真实MPS训练完成，ONNX CPU评估均通过三业务输出一致性检查。Sol独立审查数据、指标口径与实现，root逐项复核并保存失败原因。

r01公共集合明显退化，r02改善部分杯子/误报但仍有类别退化和视频误事件，均不推广为正式模型。四张现场测试共享杯实例，公共验证来自预训练COCO源，视频无完整逐帧真值；不能声称新实例泛化、现场准确率或Windows实机通过。原始图片、标签、权重、完整日志留在忽略目录。

## r02实时对照入口追加审查

- root确认单CameraSource、同一输入顺序推理、同一观察时间和fresh/status传给独立MemoryStore，故障不转为空场景；正式配置和数据未使用实验模型替换。
- 修正基线事件原先未附JPEG的问题：基线独立绘制对应检测，避免引用候选框；实际双方appeared证据存在。
- 临时停止接口要求127.0.0.1同源Origin和application/json；预览优先且标明实验模型。原始PNG稀疏留存有界，无全录像或Agent调用。
- 新测试验证推理顺序/同帧和类别数量差异判定；完整离线176通过。实时180采样结束无工作器停止错误；质量解释保留静止会话、截断手机、无移出真值和非隔离时延限制。

## r03数据与部署选优审查

- Sol人工框审与root全体预览复核；现场可辨COCO类别补全，未知白色容器帧排除。修正数据准备时仅信任审计SHA的问题，实际源视频与新增抽帧重算一致后再构建。完整视频按group隔离，训练/验证/旧test无SHA或group跨割。
- builder显式80类映射模式，保留完整标签，拒绝未知类；训练选择manifest只可引用真实val文件，检查标签清单指纹、图片SHA/尺寸/边界、train/test内容重复和group交叉。
- 依据本机Ultralytics顺序，在save_model之后评估EMA部署分支，按保存的half/contiguous/nan_to_num处理匹配序列化；只在真实epoch复制last.pt为deployment-best，最终验证回调不另算一轮。真实30条选优日志与检查点零基epoch28、experiment三处SHA共同核实第29轮来源。
- 四组等权开发macro F1只用于选优，不是准确率。旧失败/公共回归不参与epoch选择；视频后续检出不自动解释为前次missing错误，须核对画面。
- 非阻断溯源限制：experiment的dataset_sha256仅覆盖train/val，不能代表test未变化；启动隔离检查仍读取test，评价另记录各manifest SHA。回调时序缺专门模拟单测，本次以真实产物及本地源码交叉核验；不以此扩大既有测试覆盖声明。
- 完整离线185 passed/3 deselected；168输入PT/ONNX业务输出一致，三视频61采样无错误停止缺失事件，root核对4张出现证据。公共误报增加，不默认部署。所有开发子Agent为GPT-5.6 Sol。
