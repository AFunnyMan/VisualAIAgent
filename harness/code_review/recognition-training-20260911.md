# 新样本训练审查

2026-09-11，Asia/Shanghai。以下为训练前发现，未修正的数据不能训练。

- 姿态23-05-02约5—8秒抬臂伸展初标standing。root从开发联系图提出疑点，姿态Agent按0.1秒连续图复核确认仍在座，已改seated并重新抽帧；22-59-29动作组数文字改为实际5组。保留用户确认84—96秒后靠在座的真值。
- 合盖23-05-02约68—70秒初标closed，但键盘仍可见。已按连续图改为open至70秒，70—72秒排除；开发验证片局部边界亦修正，冗余closed下采样后train40closed/24open、val37closed/42open。
- 钥匙初稿把23-02-14若干帧当无钥匙负例，root在原图观察到笔记本旁仍有钥匙，已要求训练前逐帧重核负例；起始正例过于集中同一摆放，要求补充其他开发姿态。当前问题待钥匙数据修订验证，不能宣称标框完成即质量通过。
- 独立验收片按源SHA隔离，不依赖目录名；开发来源校验工具有重命名holdout拒绝测试。不能以训练/开发视频分数代替独立验收。
- 本轮LaptopTimeline不接系统睡眠、关机信号；明确partial可见证据与unknown分开。二分类的unknown不能自动桥接，先接受潜在漏报再研究，不通过改标签含义补事件。

## 训练过程中复核

- 钥匙r01第二版仍有五个验证框落在空桌面，正例框也过宽。root逐张检查26帧并另看4帧原尺寸，修正位置、不可见负例和底部截断框；CPU一轮探测及随后12轮任务在第3轮中止，错误标签对应权重和分数作废，不能用于选优/导出/质量结论。修正后重新冻结r02，30帧、11正例/19无可见，CPU固定12轮重新训练。开发正例仍高度相关且数量少，不能据此推算日常误报率。
- 合盖r01评估发现ONNX输出本身为概率，旧评估再次softmax会导致拒识；已改复用正式OnnxClassifier并补回归。r01 CPU开发验证69/79正确、7未知、3张open错closed；root全片10fps重放23-02-14产生0开合事件，未知清基线导致事件链中断。该候选未通过事件验收，准备有明确半开标注的三类r02，不放松unknown保护。
- 姿态合并数据须逐样本检查源SHA属于输入manifest的train组并拒绝路径越界；已补验证。root新增独立验收入口，检查真值SHA冻结、来源在holdout注册、训练/选优来源不包含验收源，保留10fps人工标签对照、实际人体门控、状态机和逐样本证据；工具测试不等于候选验收。

后续实际结果以训练报告和build-log为准。
