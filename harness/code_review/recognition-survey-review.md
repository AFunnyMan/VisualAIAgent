# 扩展视频识别审查

2026-09-07，Asia/Shanghai。完整证据及来源见 [评估](../../docs/recognition-assessment-2026-09-07.md) 与 [清单](../evaluations/video-survey-20260907.json)。

- 未解决：生产 YOLO26n 在大桶上持续输出高分 cup，形成出现事件；三次确认无法消除持续语义误分类。匹配关注可能因此误唤醒，当前文本 Agent 没有图像依据纠正分类。本轮仅本地事件测评，未产生云端请求或用户通知。
- 未解决：米箱桌面视频15秒手机仍可见，但连续漏检已形成 missing。现有“持续未检测到”措辞符合事实边界，但不能视为真实移出识别正确。三类现场90%目标仍未通过。
- 已修复测评问题：随机 seek 在 OGV 中依赖历史返回不同像素，POS_MSEC仍显示请求时刻。新脚本从头顺序解码；全9段重跑。手机7—21秒独立窗口与完整序列15/15检测及帧索引完全一致。生产回放原本顺序读取，此修复没有修改生产摄像头或检测参数。
- 对照检查：同一完整杯失败帧，官方 PyTorch 与 ONNX 首杯框相同，得分均约0.01265，生产阈值0.35均不检出。未观察到该帧的预处理/类别/坐标适配错误；不将单帧结论扩大到所有输入。
- 统计审查：9个独立视频、488采样帧、53视觉复核帧，计数与JSON逐项验证。标签由开发Agent查看图像给出，未由用户独立标注；每帧命中不等于每个实例正确。完全遮挡、黑场、标题卡不进入正样本分母；wine glass不混为受支持cup。随机seek探索数据已被顺序解码记录取代，定向失败复核不进入主样本分母。
- 验收边界：不是实时性能或USB验收，不扩大产品类别、不降低阈值、不删除已有测试。该轮识别不足需继续跟踪，不能沿用上一轮“仅剩用户实机验证”的概括。

调用审计显示当前业务2轮、自动事件3轮、30分钟等待0新增；115是包含失败与复验的历史开发验证总量。待机不调用模型的设计合理，进一步降费优先考虑非业务输入、重复上下文和视觉误唤醒，保留真实工具执行与通知证据检查。
