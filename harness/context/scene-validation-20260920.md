# 2026-09-20 固定场景新视频验证

用户已补录并授权检查可用后继续。现行手机漏检豁免以GOALS为准。本轮先检查内容、冻结源SHA和人工稳定区间，再对比R03/R04/R05现存部署候选，置信度统一0.35、杯复查及近重复抑制一致；不靠该片扫描epoch或阈值。

新增18-49-25约102.6秒为杯片（开头杯已在，4次完整移出，结尾杯在）；18-51-14约133秒为单手机片（3次完整移出，结尾再放入）。两片能支持开发事件比较，但分别只有一类，不能互当包含两类的开发/验收版本，也未达到原每类10次完整循环。手机远桌沿区间按最新豁免另记，不用其漏检触发专项修复。

私人输入和输出在 `data/scene-eval-20260920/`：`plan.json`在预测前冻结保守稳定在场/不在场区间和期望事件窗口，避免以模型输出来定真值；过渡帧不计稳定检出率。视频每秒采样进入真实部署检测器与EventStateMachine；不是实时USB、逐帧框IoU精度或MemoryStore/API联合验收。初始已存在杯可触发启动出现，但不能当实际放入动作。结束不足确认时长的片段单独说明。旧final-test未读取。

本批全部保持development，不进入训练；今后若需训练新框，遵守用户要求先集中人工标注复核。独立验收片仍需补充，不能随机切同片相邻帧冒充独立。先完成可用开发评估，再决定是否值得新增训练。
