# 固定场景物品 R03 修复上下文

开始：2026-09-17，Asia/Shanghai。用户要求分析并修复物品场景微调 R03，提供 development_pic，并允许最高 GPT-5.6 Sol 并行开发。不是姿态/饮水同名 R03；钥匙继续暂停。

- 新输入为 51 张 1920×1080 图片，文件名时间 14:19:24—14:23:11。整批视为同一采集会话、全部开发用途，不逐图随机拆分为独立测试。人工框先冻结并复核，再训练；预测不能充当标注真值。
- 先保存原 R03 在新图的原始单遍/杯复查输出（置信度 0.35），与人工框按 IoU 0.5 一一匹配。既有开发及失败回归不冒称盲测；未读取 final-test。
- 历史同杯 IoU≈0.987 重复框由端到端原始输出产生。修复仅同类 IoU≥0.97 的近重复框，保留相邻及普通重叠物品；正式解码和实验解码统一处理，并独立报告去重与训练收益。
- R04 计划保留 COCO 80 类头和完整可辨标签，沿用官方起点与 R03 训练参数，加入新会话全部图片；旧 train/val/test 分组保持。控制主要变量为新增样本，选优继续用原部署分支与原固定四组 val 指标。小样本训练拟合不代表真实事件质量。
- 模型与私图留在本机忽略目录，训练使用 .export-venv；不修改生产权重或本机模型配置。开发检查完成后明确仍需新连续片段/真实操作的独立事件与误报验收。
- 子 Agent：两名 Sol 分别标注前26/后25图，一名 Sol 审查并修复近重复框；root 复核、对照、训练、整合。

实际产物、训练结果和限制以 [修复报告](../../docs/scene-repair-20260917.md) 与 [实际进度](../build-log.md) 为准。

## 用户追加复核要求

2026-09-17用户要求：先把所有准备训练的人工标注图片放在同一目录，由用户再次确认。故本轮停止在87张复核包（新增51＋旧训练36），没有开始R04训练。底边仅露角的银白物体类别已询问，未确认前不标成手机；确认整批标注后再冻结数据/训练。程序去重及旧归档评估继续完成。

## 2026-09-17 21:30 +08 用户确认后训练

用户回复“确认没问题，请开始训练”，确认复核包现有标签并授权启动，不重新询问。保留底边不明物体未标注的已确认版本，不推断其真实类别。51新图标签及SHA与复核清单逐一一致，合旧36现场train和96公共train共183；val42、旧test4保持。预检查通过后从官方起点在MPS训练30轮，参数与R03相同（seed20260909、freeze10、lr0 .0002、mosaic .2）；部署分支应用本轮近重复去重，选优仍为旧四组val等权F1。新图R03基线杯22/0/5、手机27/1/18（TP/FP/FN）；新增图无瓶正例。实际结果待完成后记录。

## R04结果驱动的R05对照

R04完成30轮、选择26轮，219输入raw与部署输出PT/ONNX比较均通过；新增训练51杯27/27、手机40/45且无额外业务框，但历史old_scene四图手机从R03 4/4降为0/4。因此不部署R04，追加一次明确对照R05：以R03已训练权重为起点，lr0降至.0001，20轮，其余数据183/42、分组和部署选优42均不变。不得将四张旧test加入训练或用于epoch选择；这些历史已知失败图现在是修复诊断/开发回归，不能称R05独立测试。R04所有结果与失败保留，不覆盖历史。此对照改变初始化、学习率、轮数，不能将收益单独归因一个变量。

R05训练进行中追加事前对照：除固定42图选优权重外，同时保留并评估预定20轮的last权重。理由是旧验证不含新增机位会话，公共分组波动可能偏好过早epoch；last是训练预算事前指定终点，不扫描中间epoch或用四张旧失败图重选epoch。两者结果均报告，不改分割/阈值，不称独立验收。

## 本轮训练结论

R04选26轮、新51杯27/27手机40/45零额外，old_scene手机0/4；R05选15轮与固定最终20轮均新51杯26/27手机38/45零额外，old_scene手机2/4。三候选各219输入raw/部署输出导出一致性通过，三段61采样开发回放保留出现事件、无空场出现或停止missing。当前新图开发优先R04，但不生产替换；两轮全部结果、旧图退步与剩余遮挡漏检保留。[实际训练结果](../../docs/scene-r04-r05-results-20260917.md)。停止无依据重复训练，下一步是冻结候选后的新同场景连续验证。
