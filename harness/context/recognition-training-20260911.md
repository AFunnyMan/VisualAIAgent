# 固定场景新样本训练

开始：2026-09-11，Asia/Shanghai。用户确认新素材与躺靠真值后明确要求开始开发训练。恢复识别质量、性能优化，并训练钥匙/被观察笔记本合盖。子Agent最高Sol；训练依次使用本机隔离环境，数据处理可并行。

## 数据决策

- 冻结来源见 `harness/evaluations/recognition-20260911-sources.json`。七段无验收前缀为development；含抱臂后靠失败姿态的23-05-02已明确转开发。
- `验收2026-09-11 23-08-00.mkv`及派生帧禁止训练和选优。只完成输入质量目检，候选冻结后再评价。source-policy工具依据SHA而非文件名拒绝holdout及未注册新源。
- 抱臂后靠和不抱臂后靠均为在座。不可见/过渡不凭动作计划标真值。少量固定场景样本不能推断长期误报率。
- 姿态与旧数据混合；钥匙优先独立单类实验头，保持原三物品模型；合盖优先局部分类，模型都隔离保存，不自动部署。

## 开发与保护

- `LaptopTimeline`独立实验时序：open/closed确认、同次事件去重、启动基线、故障/unknown/场景变化清除上下文。partial只能来自正面可见证据，不能把低置信unknown改称partial；明确partial最多保留3秒转换上下文。模型若只输出open/closed，unknown仍清基线，可能漏事件也不得补造。
- 正式行为线程禁用未被使用的每帧诊断图复制/哈希，实验入口默认保留。源画面、分类、事件证据和250ms时效检查不变；微基准不等于端到端长尾通过。
- 精度/事件回放、ONNX数值与CPU测试完成后才能讨论启用；样本缺口保留，不靠调独立验收阈值通过。

实际训练参数、结果及剩余工作由各训练报告、训练日志和build-log记录。
