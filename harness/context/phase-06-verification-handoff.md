# 阶段 06 上下文

2026-09-07，Asia/Shanghai。各功能模块已实现；root 执行总集成、文档、锁定安装、原生 CI 配置及本机浏览器验证，保留真实模型/摄像头/Windows 11 条件。

持续验证分为：假时钟/SDK假模型的离线回归、真实ONNX公开图片对比、实际十分钟静态图片回放、真实模型与USB验收。前面三种均不能替代后者。短图片推理benchmark记录CPU和RSS但不宣称摄像头性能或8GB基线通过。

Mac/Windows启动脚本、doctor、benchmark和offline_soak已经建立；详细用户辅助步骤集中在 docs/user-acceptance.md。脚本默认数据写入被忽略目录。项目不部署、不创建安装包、不引入Docker。

准备Git里程碑时只提交源码/测试/配置/脱敏摘要/原许可证，模型权重和运行数据保持忽略。原始计划基线SHA不变。最终命令与状态追加到build-log，不以正在变化的中间测试数替代最终证据。
