# 后续任务入口

这些提示词用于用户主动启动后续任务，不会自动执行。本次文档准备完成后停止。阶段序号与依赖见 [PLANS.md](PLANS.md)，实际状态见 [build-log](harness/build-log.md)。

## 1. 了解项目（只读）

阅读适用 AGENTS.md、GOALS.md、PLANS.md 和 build-log，再检查真实文件与 Git 状态。总结已实现能力、尚未开始的阶段、阻塞和下一步建议。区分文档承诺与实际证据，不修改文件，不运行付费 API 或摄像头。

## 2. 准备一个阶段（只更新计划）

准备阶段 NN。先阅读该阶段文件、architecture、testing 和相关 context，进行只读现场核对。仅更新本阶段实施计划中已确认的接口、路径、验证入口、依赖与恢复步骤；重大范围变化先说明。不要开始业务实现，不预填阶段 context 或通过结果。检查文档链接与差异，在授权范围内提交本次计划更新后停止。

## 3. 实施一个阶段

开始执行阶段 NN。阅读适用 AGENTS.md、目标、路线图、本阶段计划和最近实际进度。核验前置能力后，在该阶段范围内实现最小完整行为，运行相关验证并审查修复；记录真实命令、结果与限制，更新日志和必要 context。遵守用户的重要步骤提交要求。完成本阶段后停止，除非我明确授权继续后续阶段。前置条件不足时继续可独立完成的范围，保留待验收项，不虚报完成。

## 4. 验证并收集证据

验证阶段 NN。读取阶段验收与 testing，检查命令已实现后执行适当的离线验证。真实 API 或摄像头验证仅在任务范围覆盖且条件具备时执行。不要凭空补齐用量、平台或识别效果。将实际结果写入日志，报告通过、失败、未运行和模拟结果的区别。发现实现缺陷时记录可复现证据，不在仅验证任务中扩展产品功能。

## 5. 审查与修复

审查阶段 NN 的当前改动，重点检查目标映射、工具是否实际执行、数据新鲜度、提醒幂等、错误恢复、成本边界和测试真实性。把发现、证据和修复建议写入本阶段审查文件。若本任务明确包含修复，在原阶段范围内修复并重新验证，保留原发现和关闭依据；否则交付审查后停止。

## 6. 恢复工作

从仓库恢复开发。先读取适用 AGENTS.md、PLANS.md、build-log 和未完成阶段计划，再读取该阶段 context 与审查记录，并核验当前代码和 Git 状态。识别已通过检查、未验证项、上次中断位置及我的当前授权范围。若我授权继续实施，执行下一条已明确且可验证的动作；若只要求了解进展，保持只读。不要重做已验证的完整阶段，也不要将失效证据当作当前通过。

## 授权连续实施时的补充

若用户明确要求“连续实施整个项目计划”，阶段验收和审查通过后直接进入下一个阶段。每次切换记录可观察结果与下一阶段；只有范围变化、必要条件缺失或不可替代的外部操作需要额外输入时提出具体问题。此约定不覆盖当前“先创建文档，后续主动开始”的范围。
