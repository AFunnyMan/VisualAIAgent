# 1. 基于 AI Agent 与本地机器视觉的桌面视觉记忆智能体

## 1.1 项目描述

VisualAIAgent 是一个具备视觉记忆查询、工具调用和条件触发提醒能力的桌面 AI Agent，面向固定桌面场景中的物品查找、行为观察与日常提醒。用户用自然语言表达需求，Agent 将需求转化为受控业务工具调用，查询真实视觉记录、建立持续关注任务，并依据执行结果回答或提醒。项目围绕“理解需求、调用工具、获取证据、执行任务、反馈结果”构建完整流程，也可用于大语言模型 Agent 与机器视觉结合的教学和工程实验。

例如，用户询问“最后在哪里看到手机”时，Agent 调用物品查找工具，获取最后出现时间、画面左中右区域和截图证据，再组织可追溯的回答；用户提出“杯子出现时提醒我”时，Agent 调用任务创建工具，将一次对话转化为持久化关注任务。任务等待期间由本地程序持续检查视觉事件，满足条件后唤醒 Agent，由其调用通知工具写入页面提醒。这样，系统既能响应即时提问，也能在用户提出需求后继续跟进，并在后续事件发生时提供反馈。

本地机器视觉为 Agent 提供持续更新的事实来源。普通 USB 摄像头采集桌面及座位区域，YOLO 模型与程序状态机将画面整理为物品位置、状态变化和带有截图的事件记录，SQLite 保存可查询的视觉记忆。Agent 使用这些结构化事实完成推理与操作，持续视觉处理留在本地，避免逐帧调用云端大模型。

系统还接入了在座、站立、空座及疑似饮水的实验识别模块，通过连续观察确认起身、坐下、离座等事件，统计累计在座时长、连续有效在座时长、离座次数与时长，以及疑似饮水次数。在此基础上，用户可以配置连续在座休息提醒，或建立“18 点后离座，且手机仍在画面右侧时提醒”等组合规则。规则判断与计时在本地完成，只有用户提出请求或提醒条件满足时，才调用大语言模型处理相关任务。

项目当前服务于固定机位和固定使用场景。行为识别仍属于实验能力，在座时长不等于学习或专注时长，疑似饮水也不代表已经吞咽或能够估算饮水量。笔记本开合已完成候选训练及程序接口接入，但仍需补齐可见性判断和实机验收，当前保持关闭；钥匙检测暂缓。摄像头断连、画面过期或目标无法确认时，系统显示未知，不根据缺失画面推断物品消失、人员离座或电脑合盖。

## 1.2 技术栈介绍

### 1.2.1 OpenAI Agents SDK

OpenAI Agents SDK 用于构建项目的 AI Agent，解决自然语言需求如何转化为实际业务操作的问题。项目将当前场景查询、物品查找、事件搜索、任务创建、任务列表查询、任务取消和通知写入封装为七个受控工具，由 Agent 根据用户需求选择调用，并读取执行结果后组织回答。例如，用户询问手机的位置时，Agent 查询视觉记忆并返回时间、区域和截图证据；用户要求杯子出现时提醒，则调用工具创建持久化关注任务。任务创建和通知发送以工具实际执行结果为准，使智能体能够完成可核对的操作。

### 1.2.2 阿里云百炼 · 千问大模型

千问大模型为 Agent 提供自然语言理解、工具选择和回答生成能力，解决用户口语化表达与结构化业务接口之间的转换问题。模型通过工具获取必要的文字事实、时间和证据编号，再据此解释物品记录、行为统计和任务结果。用户主动提问或本地提醒条件满足时才调用模型，持续视频识别和任务等待在本地完成，从而控制云端调用频率，也避免持续上传摄像头画面。模型只通过限定的业务工具访问项目数据，不具备任意 SQL、Shell 或代码执行能力。

### 1.2.3 YOLO26n

YOLO26n 用于将摄像头图像转化为可供业务使用的视觉结果，解决桌面物品类别、位置及固定场景姿态的识别问题。物品检测模型输出手机、杯子、瓶子等目标的检测框与置信度，程序据此划分画面左、中、右区域。行为模块采用分别处理姿态和疑似饮水的分类模型，使在座与疑似饮水可以同时成立，并结合人体检测进行辅助判断。固定场景模型通过本地视频标注和预训练模型微调获得；行为能力仍属实验阶段，笔记本开合候选模型尚待验收，钥匙检测暂缓。

### 1.2.4 OpenCV

OpenCV 用于接入 USB 摄像头、读取本地测试视频和完成图像预处理，解决硬件画面如何稳定进入模型的问题。项目通过共享采集源向物品与行为分支提供最新画面，避免多个模块重复打开摄像头；模型推理前完成图像缩放与填充，推理后还原检测框坐标。相同的视频读取接口也用于开发回放，让已有素材能够重复验证。采集故障和过期帧由配套程序检查，防止把失效画面当作当前事实。

### 1.2.5 ONNX Runtime

ONNX Runtime 用于执行导出的 ONNX 视觉模型，解决模型从训练环境迁移到日常运行环境的问题。正式推理采用 CPU 执行方式，使普通电脑能够运行视觉分支，不将独立显卡作为必需硬件。训练、导出与正式运行使用隔离环境，并核对导出前后的输出一致性及模型版本，降低实验依赖和权重变更影响日常程序的风险。当前已在 Apple 芯片 Mac 上开展验证，Windows 11 x64 的实际运行效果仍需实机验收。

### 1.2.6 Supervision

Supervision 用于统一表示检测结果并辅助绘制检测框和标签，解决模型输出不便直接查看与核对的问题。页面将目标位置、类别等结果叠加到画面上，方便用户检查程序识别的是哪个物品，也方便开发时对照误检与漏检。检测框是位置证据，同类多个物品仍作为候选展示，不据此推断具体物品身份。

### 1.2.7 Python 3.11

Python 3.11 承载视觉处理、业务服务、动作状态机和本地情境规则，解决各模块如何协同运行以及连续识别结果如何形成可靠事件的问题。项目采用单进程、多线程架构，由统一运行时管理采集、识别和 Agent 请求队列。程序状态机根据连续证据确认起身、坐下和离座，处理短暂未知及采样中断；本地规则在事件发生时检查时间、物品区域和事实有效性，满足条件后再唤醒 Agent。有效时长累计、每日统计切分及重复事件处理也在本地完成，避免按单帧波动生成动作或把断流时间补算为在座。

### 1.2.8 SQLite

SQLite 为 Agent 提供持久化视觉记忆和任务记录，解决画面消失后无法追溯、程序重启后任务与统计丢失的问题。数据库分别保存物品观察、行为事件、有效状态区间、对话、关注任务、情境规则和通知，事件通过证据编号关联本机截图。Agent 可通过业务工具查询历史事实，回答“最后在哪里看到”等问题。事务、版本校验和去重逻辑共同支持任务恢复与通知一致性；规则修改或取消后，旧版本排队任务不能继续发送提醒，数据库迁移前执行备份。

### 1.2.9 Streamlit

Streamlit 用于构建本地交互页面，解决用户如何查看识别结果、使用 Agent 和管理提醒任务的问题。页面集中提供实时预览、自然语言对话、事件截图、行为统计、关注任务、情境规则及调用记录，让用户既能提出需求，也能核对 Agent 使用的证据和执行结果。页面通过缓存运行时访问后台服务，避免刷新时重复启动摄像头或识别线程，并展示模型不可用、未知状态和本地降级通知等信息。

### 1.2.10 uv

uv 用于管理 Python 依赖、隔离环境和版本锁定，解决不同机器安装结果不一致，以及训练与正式运行依赖相互影响的问题。项目依据锁定依赖准备运行环境，将模型训练和导出环境与日常程序分开，便于复现已验证的配置和定位版本问题。

### 1.2.11 pytest 与 Ruff

pytest 用于验证状态机、统计、数据库事务、任务恢复、规则版本和通知去重等关键行为，Ruff 用于静态检查，帮助在迭代中发现逻辑回归和代码问题。项目还通过视频回放、模型导出一致性检查及真实千问受控文字测试核对工具链。自动化测试用于验证程序流程；固定场景识别质量、新模型与真实摄像头的联合效果，以及 Windows 实机表现仍需分别验收。

## 1.3 硬件平台介绍

本项目采用普通电脑与 USB 摄像头组成视觉观察平台。当前已在 Apple 芯片 Mac 上开展开发、模型训练、本地推理和 USB 摄像头验证；Windows 11 x64 为目标运行平台，具体设备上的安装、采集和持续运行效果仍待验证。正式推理采用 CPU 执行方式，不将独立显卡或专用 AI 加速板作为必需硬件。

摄像头用于采集桌面物品、座位及上半身活动区域，可根据设备能力请求 640×480、1280×720 或 1920×1080 等分辨率，并记录实际获得的画面尺寸。现阶段使用固定机位，需保证常用物品、座位区域及相关动作尽量处于可观察范围内。改变摄像头位置、取景范围或光照条件后，应重新检查识别效果。目标被完全遮挡或离开视野时，画面不能证明其实际去向，物品事件仅表达持续未检测到，不推断物品被谁拿走。

模型运行主机需要在观察期间保持开机，摄像头画面仅在本地处理，模型、SQLite 数据库和事件截图保存在本机。仅在访问千问服务时需要网络连接，持续视觉处理和本地规则匹配不依赖逐帧云端请求。被观察的笔记本与模型运行主机是不同角色，后续验证合盖场景时，运行主机仍需持续工作。实际部署还应检查 USB 连接稳定性、磁盘空间、CPU 与内存占用，以及连续运行时的散热和延迟表现。

*项目状态截至 2026 年 9 月 12 日。*
