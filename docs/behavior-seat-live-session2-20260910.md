# 座位关联第二次实机测试

2026-09-10，Asia/Shanghai。用户明确开始实测，并确认完成两轮“坐稳5秒→站稳3秒→坐下5秒”，随后离开画面约5秒并返回坐下。

**第二轮坐下仍漏事件，新旧逻辑输出完全相同，当前改进未通过这次实机验收。** 在座状态能恢复；不能把状态恢复当作坐下事件正确记录。

## 实际动作与输出

根Agent逐张核对7张事件图，并核对漏报附近7张连续诊断图。时间为本地采集时间，四舍五入到毫秒。

| 环节 | 实际输出 | 结论 |
| --- | --- | --- |
| 首次进入座位坐下 | 17:52:18.913 sat_down | 图片与坐下外观相符，属于准备阶段 |
| 第一轮起身 | 17:52:30.177 stood_up | 有记录 |
| 第一轮坐下 | 17:52:35.042 sat_down | 有记录 |
| 第二轮起身 | 17:52:43.281 stood_up | 有记录 |
| 第二轮坐下 | 17:52:48.754恢复seated，但没有sat_down | 漏报；诊断图可见身体下降并坐下 |
| 起身离开 | 17:52:52.641 stood_up；17:52:55.459 left_seat | 两个事件均有记录，离座图为空椅 |
| 返回坐下 | 17:53:02.098 sat_down | 有记录 |

按用户确认流程，两次循环坐下只记录1/2，返回坐下1/1，三次起身均记录，离座1/1。准备阶段另有一次坐下。不以这几个动作估计总体准确率或误报率；本轮未执行饮水、弯腰或遮挡反例，也没有全程独立人工逐帧标注。

## 漏报链条

1. 17:52:47.393（frame1158）仍为已确认站立；下一次采集17:52:47.473的推理直到17:52:48.041才记录为stale，错误为`Inference completed after the frame expired`。无效结果按规则清空状态上下文。
2. 下一张新鲜frame1160采集于17:52:48.034，与上一新鲜画面相隔约0.6405秒，超过0.25秒保护界限。1160—1162的原始站立证据只持续约0.208秒，未重新满足0.3秒站立确认。
3. frame1162的人体候选中有两个匹配框，分数0.4536/0.4457，新关联拒绝为`multiple_spatial_matches`；旧分支也因没有≥0.5强框而拒绝。诊断画面中只有一个主要人体，提示可能存在同一人体的重复/分裂候选；不能由框数直接判断两个人。
4. frame1163分类unknown，1164开始seated，1167满足在座确认，但此前站立上下文已丢失，所以新旧分支都不发sat_down。

这是“过期结果导致连续证据中断，随后没有足够时间重建站立”的实际失败；候选歧义也存在，但不能把全部原因归为检测置信度。不能通过取消故障重置来补造事件。过期推理内部各阶段耗时目前未完整记录，暂不能断言是摄像头、ONNX执行、线程调度或证据写盘造成。

Sol只读复核并对该片段做内存反事实重放：即使仅把1162的人体连续性支持改为True，仍无法恢复sat_down，因为故障已经清空前态，随后站立时长不足。该诊断没有修改程序或原始记录，不算修复测试通过。

## 性能与证据

实际使用[上一轮命令](behavior-seat-improvement-20260910.md)，仅输出目录改为`data/acceptance/behavior-seat-live-20260910-session2`，300秒上限。初次900秒参数被CLI拒绝，未启动摄像头，改为300秒后成功；没有改模型或参数。

17:50:35启动，首末新鲜画面17:50:43.023—17:53:36.803，共约173.78秒、1624次新鲜采样，9.34fps；6次过期结果安全拒绝，73次启动等待。用户确认完成后同源POST正常停止，17:53:37.497结束，进程退出0、worker_stop_error为空；不是完成300秒上限。新鲜推理p50/p95=73.782/88.638ms。该分布排除了过期失败样本，不能用于声称全部推理无长尾。

私人事件PNG 7张、诊断静态图70张（8组）、逐帧日志和模型/代码SHA保存在上述忽略目录；没有完整录像或云端调用。脱敏记录见[汇总](../harness/evaluations/behavior-seat-live-session2-20260910.json)。本次代码未修改，不重复全量回归；上次269项通过仅说明原有自动测试通过。

## 后续工作

优先给失败路径补分阶段耗时和故障前后有界诊断，确定约0.57秒延迟发生在哪里，再针对实际瓶颈处理。人体候选另做重复框/真实多人对照，不能无条件取最高分框。保留过期和遮挡保护；修复后重新进行坐站及反例实测，现阶段没有证据要求立刻重训分类器。
