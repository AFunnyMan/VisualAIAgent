# 行为方案真实摄像头测试

2026-09-10，Asia/Shanghai。用户要求测试r03候选，并同意页面准备好后配合一次坐稳、起身、离开、返回、杯口到嘴及拿杯不喝流程。当前测试与正式产品数据隔离，沿用[候选及限制](behavior-improvement-r03-20260910.md)。

## 启动前实际检查

- 未发现其他Streamlit或摄像头测试服务运行。
- 第一版临时探针把VisionWorker位置参数顺序写反，未打开摄像头；改用具名参数后重新执行。
- CameraSource(0)、1920×1080、VisionWorker、0.1秒间隔、0.25秒过期阈值，8秒短探针取得17个新鲜观察；AVFOUNDATION，实际1920×1080，正常停止并退出0。初始化等待期出现stopped以及一次stale，不能将整个启动等待说成断连或稳定运行。
- root目检第一张本地新画面：人物坐着，手持手机，未见杯口到嘴。原r02 ONNX静帧推理输出seated 0.9997、not_drinking 0.9895，仅是一次核对，不是准确率或连续事件验收。
- 同一缓存图片的CPU小基准（含预处理，姿态、饮水、person三个ONNX顺序执行，10次预热＋50次计时）：p50 66.66ms、p95 74.42ms、max 255.90ms。不是应用帧率；存在慢样本，实时必须继续执行陈旧帧与间隔保护。

探针图片、原结果和性能数据留在本机忽略目录 `harness/artifacts/behavior-live-20260910-probe`。没有调用项目云端模型，没有修改正式物品模型或生产数据库。

## 真实运行结果

新增独立入口 `scripts/behavior_camera_test.py`，复用CameraSource与VisionWorker，由采集线程独占打开/读取/释放；仅ONNX CPU，无Torch或云端Agent依赖。训练manifest、实际权重SHA、类别和张量形状校验后启动；person gate和状态机均使用真实采集时间戳。过期、未来、重复帧拒绝，启动等待单列计数，正常停止可退出0。

实际命令：

```sh
.venv/bin/python -m scripts.behavior_camera_test --posture-manifest harness/artifacts/behavior-20260910-r02/posture-run/training-manifest.json --drinking-manifest harness/artifacts/behavior-20260910-r02/drinking-run/training-manifest.json --output data/acceptance/behavior-r03-live-20260910-session1 --duration 300 --camera 0 --width 1920 --height 1080 --port 8765
```

16:08:19启动，首个新鲜画面16:08:25.154、末个16:11:47.652，16:11:48.122停止。目标上限300秒，动作流程完成后通过同源POST `/stop` 提前正常结束，进程退出0，worker无停止错误；不是完整5分钟测试。期间通过真实浏览器打开页面，看到实时计数、状态和停止按钮。

| 实测项目 | 结果 |
|---|---|
| 新鲜观察/有效时长 | 1942次／约202.50秒 |
| 平均处理帧率 | 9.59fps |
| 启动等待观察 | 56次，单列、不当离座 |
| 新鲜画面开始后的过期观察 | 4次，均为发布前画面过期 |
| 最大新鲜帧间隔 | 0.441秒，超过0.25秒共4次 |
| 三模型预处理及推理p50/p95 | 73.23／78.60ms（只统计成功新鲜结果） |
| 发布前画面年龄p95 | 99.73ms |
| 真实摄像头读取断连 | 本轮未观察到；不等于重插恢复验收 |

用户第一次“已完成”随后明确更正为误回复，那段无动作切换不计失败。之后用户确认完成离座返回及饮水/拿杯反例流程，根Agent核对事件图片、少量定时画面和逐次分类日志。

| 动作或状态 | 观察结果 |
|---|---|
| 起身 | 16:09:34.604记录stood_up，截图为站立 |
| 离座 | 16:09:36.486记录left_seat，截图为空座 |
| 返回在座 | 已恢复seated |
| 坐下事件 | **漏报1次**，未记录sat_down |
| 疑似饮水 | 16:10:24.237记录一次，图片为杯口接近嘴；不证明实际吞咽 |
| 拿杯不喝 | 用户确认完成，后续无额外饮水事件；未保留精确负例动作画面，不能独立计分为负例通过 |

坐下漏报定位：16:09:44.965已经确认standing；16:09:47.012姿态分类短暂unknown，同时person gate为`not_one_strong_person`，历史standing被清除；16:09:47.124分类恢复seated，但人体门控尚需前帧，16:09:47.444恢复确定在座，却没有旧standing上下文生成sat_down。这不是摄像头断开，也不能通过跨所有unknown强行桥接来修补。该门控记录未保存所有原始候选，无法进一步断言当时是零个还是多个强人体框。

本轮3个事件均有精确关联截图；另外24张定时审查图来自分别轮询的预览和状态，关联为近似，不能当逐帧真值。定时饮水审查启动晚于实际动作，未捕获完整正负例过程。没有完整录像，私人图片及运行日志均在忽略目录；[脱敏记录](../harness/evaluations/behavior-live-20260910.json)保存结果、事件与文件SHA。

新入口10项定向测试通过，实机结束后全离线回归256 passed、1 skipped、3 deselected（10.76秒）。Sol只读复审同帧对应、新鲜度、故障重置及停止路径，无当前实测阻断问题。未改正式模型与时序参数，没有把本轮真实素材用于重训或现场调阈值。

结论：真实在座/空座链路、起身、离座及疑似举杯事件已观察到，但坐下仍有明确漏报，行为启用质量未通过。下一步应先保留失败采样附近的有界证据、区分人体候选缺失与多人体，再验证座位区域/时序连续性改进；不应仅降低置信阈值或把人体框当无遮挡证明。仍缺更充分的独立动作、精确反例边界、遮挡、长时误报与Windows验证。
