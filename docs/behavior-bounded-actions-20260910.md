# 修复版真实动作验证

2026-09-10，Asia/Shanghai。用户要求重新打开，并回复动作完成。源会话为`data/acceptance/behavior-bounded-actions-20260910-184555`，不是此前无人配合的稳定性测试。

## 已核对结果

根Agent目检全部5张事件图，均与相应外观状态相符；另抽查早期5组诊断图，均为在座。实际采集时间（UTC+8）：

| 时间 | 事件 |
| --- | --- |
| 18:47:20.216 | 起身 |
| 18:47:25.590 | 坐下 |
| 18:47:32.183 | 再次起身 |
| 18:47:33.944 | 离座 |
| 18:47:41.015 | 返回坐下 |

新旧人体关联分支输出相同。用户随后明确本次实际执行“一轮坐5秒→站3秒→坐5秒，再离开5秒后返回坐下”。据此预期为起身、坐下、起身离开、离座、返回坐下共5事件，本轮5/5全部记录，未发现额外事件，两个坐下均成功。该结果只表示这次确认的短流程通过，不是总体准确率或完整行为验收；没有实际执行第二轮完整坐站，不能将原计划多出的步骤计成漏报。

## 运行及故障

18:46:36.975启动，首末新鲜18:46:43.531—18:48:16.198，共约92.667秒、881 fresh，3 stale、61 startup。用户回复后同源POST停止，18:48:16.495结束，进程退出0、worker_stop_error为空。不是跑满300秒上限。

全部detect耗时（含失败）中位42.942ms、p95 61.283ms、最大404.493ms。人体辅助ready864/timeout11/busy9；饮水ready881/timeout3。这些计数包含被主帧拒绝的尝试，不能全部算有效采样。

3次过期均在本次已记录动作区间18:47:20—18:47:41之外：

- 18:47:09.031帧：detect142.2ms，JPEG编码阶段累计233.6ms，发布前年龄391.2ms，拒绝。
- 18:47:09.198帧：推理前画面已经225.5ms，姿态ONNX141.1ms，detect149.0ms，发布前年龄382.2ms，拒绝。
- 18:47:47.708帧：姿态ONNX228.7ms，复制/哈希163.4ms，detect404.5ms，发布前年龄425.5ms，拒绝。

故障后在座状态恢复，没有据过期画面生成离座。人体/饮水等待隔离已生效，但主姿态、编码、内存复制或系统调度长尾仍可使整帧过期；因此原来的“5分钟0过期”只适用于那段已记录测试，不能推广为故障已消除。当前计时不能区分函数内部计算与线程被挂起的时间，不能断言硬件或某一算子是唯一根因。

图片和完整逐帧日志留本机忽略目录；[脱敏汇总](../harness/evaluations/behavior-bounded-actions-20260910.json)保存事件、时间、SHA和用户确认的流程。未修改代码或权重，不重复全量回归，云端调用0。此次短动作流程通过，整体行为质量暂不宣布通过；后续重点处理发布链路长尾，并补多次坐站、自然反例和长时运行。
