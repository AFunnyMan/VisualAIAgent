# 当前透明杯漏检：同帧缩放复查

2026-09-08，Asia/Shanghai。承接[摄像头复验](camera-remediation-2026-09-08.md)。用户已调整摄像头并放回杯子，本轮继续实测和修复。当前杯子的静止检出得到改善；换姿态、移出/放回及三类整体准确率仍须独立验收。

## 问题与资料依据

21:46:47—21:47:47 页面记录60个新鲜采样、杯子检出0次。用户确认放置，附近页面画面及随后原始照片可见完整杯子；这支持持续漏检诊断，不是对60张原图逐帧标注的正式召回率。采集设备为用户授权的OBSBOT摄像头0，1280×720；原观察范围为880×420桌面裁剪。

Ultralytics 的 YOLOv5 TTA 文档用不同缩放和翻转输入进行复查，并明确额外计算成本。这里只借鉴多尺度输入的思路，自己对已验证的YOLO26n ONNX输入作一次有界变换；并未声称该ONNX支持YOLOv5的`augment`参数或能复现其数据集收益。[官方TTA说明](https://docs.ultralytics.com/yolov5/tutorials/test-time-augmentation)

切片推理强调输入尺度和跨切片坐标合并，但当前杯子并非仅因像素太少而失败；真实尝试应决定采用哪个方向。[SAHI官方切片说明](https://github.com/obss/sahi/blob/main/docs/guides/sliced-inference.md)。CLAHE可改善局部对比度，不保证提高检测能力。[OpenCV说明](https://docs.opencv.org/4.13.0/d5/daf/tutorial_py_histogram_equalization.html)

## 有界开发试验

暂停页面摄像头后保存6张原始PNG。只上传查询文字查资料，私人画面、数据库和权重不上传、不入Git。以下参数搜索都属于开发试验，不是独立测试集：

- 首张图7种完整/裁剪上下文×4个旋转，共28次YOLO26n推理。0.05仅用于观察低分候选，正式接受门槛仍0.35；没有一种恢复真实杯子到正式门槛。
- 完整图/原ROI分别尝试水平翻转、灰度、CLAHE、模糊、3个gamma、0.5/0.75缩小填充，共18种。只有完整图缩小0.75、居中填充114有效，其余均无正式三类输出。
- 将0.75冻结后，六张开发图全部检出真实杯子，分数0.411—0.446。缩放后的框必须逆变换回原画面，不能直接画在原图上。
- 独立Sol子任务比较首末两张的现有模型：m全图两张杯子约0.538/0.506；RF Nano全图/ROI失败、重叠切片两张成功；s全图/ROI失败，切片局部将杯误为瓶。更大模型没有被接入生产；保留既有n、模型清单和SHA校验。

## 固定参数后的新鲜画面对照

22:04:55起使用正式CameraSource、VisionWorker、MemoryStore，另加私有同帧对照包装器；先原图n，再对同一新鲜图做0.75复查，仍用0.35。冻结后不再根据结果调整参数，120个新鲜采样中原图0次检出杯子，缩放复查120次。仅产生一次cup appeared，没有cup missing，采集故障0，工作器正常关闭；末次原始快照已目检杯子仍在。

两次推理合计p95约134.43ms；含启动的进程CPU均值31.10%（单核100%），观察到的峰值RSS421.23MiB。该时段有其他本地开发活动，这些是同帧诊断运行的资源观察，不是隔离性能基准。不能把120/120称为多场景100%准确率或物品移动闭环通过。

辅助脚本在已保存结果、已停止摄像头后误调用不存在的`MemoryStore.close()`，最终退出1；这条清理异常如实保留，不能把整个命令记为退出0。错误调用已移除；该异常不在生产代码中，摄像头关闭和120条结果均有实际证据。

## 可选功能的边界

`VAA_CUP_SCALE_RECHECK=false`为仓库默认；页面提供“增强杯子检测（本地复查）”。启用后：

1. 原图正常检测；已有杯子则跳过复查。没有杯子才额外做一次0.75缩小、居中114填充。
2. 复用同一个通过清单/SHA校验的n模型，只接纳复查中的cup，保持原置信度门槛；原图的瓶/手机结果保留。
3. 按实际缩放后的宽高分别逆映射，裁剪回原图；纯填充区和退化框丢弃。预览与证据仍为真实原图，不显示填充图。
4. 一张图最多两次本地推理，但仍只是一条观察。没有跨帧缓存检测，不降低三次出现确认/五秒有效缺失规则。慢推理、暂停、过期和断连继续受原工作器保护。
5. 开关更改须停止旧工作器后生效，并重新建立当前事件基线，保留历史。相同设置重复开始不启动第二个工作器。验收台、性能脚本和诊断配置同步支持此开关。

这项复查对当前观察画面生效；本次有效组合为720p完整画面，而非此前桌面ROI。不能假定任意裁剪和缩放都有效。它也不替代类别区分能力，既有桶/锅误识为杯仍可能发生。

私人证据位于`harness/artifacts/cup-slant-20260908/`：`inputs/`、`geometry.json`、`photometric.json`、`frozen-scale.json`、`live-scale/`及`model-trial/`。软件回归、困难样本组合计分、正式页面实测与提交结果见[实际进度](../harness/build-log.md)。

## 困难样本的实际组合检查

在23张既有COCO困难图、6张已复核桶/锅视频关键帧、1张桶静态图上按真实条件重算：13张因原图已有cup跳过复查，17张复查均未追加cup。原/组合的总体TP/FP/FN均为32/25/43，cup均为14/16/12（同类IoU≥0.5）。没有新增正确杯子或假杯子；四张锅桶视频的原cup误报全部继承，不能称消除了误报。单独缩放分支在遥控器图中的假手机因只接纳cup而未进入组合。此30图刻意选择困难样本，不能用于估计日常误报率。

可分享的计数、固定规则及私人证据SHA见[结果摘要](../harness/evaluations/cup-scale-recheck-20260908.json)。

## 正式页面复验

本机私有设置切换为1280×720完整画面、增强开启，重启原Streamlit服务后从侧栏启动。22:12:52—22:14:51的首120个新鲜观察中，110个检出杯子（91.7%采样命中）；最长连续未检出仅1个，只有一次cup appeared，无cup missing，采集故障0，最大采样间隔1.094秒。页面目检框落在真实杯子上。实际页面推理p95为177.76ms，此时还有测试/开发活动，不是隔离基准；Agent运行0次。

这个结果比独立同帧对照的120/120更低，须同时保留，不能只报告最好的一轮。它证明当前静止姿态明显改善，不能证明所有放回姿态都稳定。已请求用户进行移出10秒、放回保持15秒；截至本条尚无用户完成确认，不将该闭环记为通过。
