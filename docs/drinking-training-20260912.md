# 饮水 r03 固定场景改进

2026-09-12，Asia/Shanghai。仅识别疑似杯口到嘴，不推断吞咽、水量或学习质量。模型仍为实验候选，正式默认权重未替换。

## 数据与训练

对 23-03-14 开发片按 0.5 秒顺序图人工复核，明确到嘴与未到嘴区间提取 189 帧（33 正、156 负），与旧 508 帧合并为 697 帧（88 正、609 负）。边界和不清楚的区间不强制分类；整段开发视频已用于训练，不能当独立验收。新增图片已加入本机 `data/标注复核-20260912/打开查看.html`。

固定预训练 YOLO26n-cls、320 输入、20 轮、MPS、batch 8、freeze 5、seed 20260912，训练约 661.96 秒。训练 697/697 拟合只检查流程；PT/ONNX 最大绝对差 1.19e-7。候选 ONNX SHA256：`3b3cb250670a95c7af9f5243e601ccd8461cee4ffde582c6ac194cf98bb37dca`。

## 连续回放结果

使用相同 r04 姿态、人体辅助与正式动作状态机，10 fps 回放，未放宽确认门槛。未知/未标注样本不计作错误真值。

| 范围 | 旧模型事件 TP/FP/FN | r03 事件 TP/FP/FN |
|---|---|---|
| 参与训练的 23-03-14 开发片 | 4 / 2 / 0 | 4 / 0 / 0 |
| 未参与饮水训练/选优的验收片 | 1 / 0 / 0 | 1 / 0 / 0 |

开发片 475 个有明确真值的采样，r03 全部接受且分类正确；其余不评分。留出片先顺序人工标注和冻结真值、权重/清单 SHA，再读取预测；同源训练检查通过。此片此前已用于姿态和离座诊断，因此这里只称**饮水模型留出检查**，不称整个产品独立验收。

留出片 28 个明确到嘴采样，r03 全部接受正确；1,217 个明确未到嘴采样，r03 为 1,155 正确、56 未知、6 错饮水，旧模型为 739 正确、393 未知、85 错饮水。另 522 个过渡/不清楚采样不评分。r03 仍有短暂错误帧，状态机未把这些帧形成额外事件。仅一次正动作和一段短视频，不能据此宣称零日常误报率。留出 PT/ONNX 最大差约 1.25e-5。

## 复现与后续

训练入口 `scripts.train_behavior_model`；回放入口 `scripts.evaluate_drinking_revision`，开发真值见 `harness/evaluations/drinking-replay-truth-20260912-r03.json`，留出真值及冻结文件留在本机 `data/quality-review-20260911/holdout-drinking-truth/`。脱敏结果见[结果 JSON](../harness/evaluations/drinking-20260912-r03.json)。完整画面、模型和逐帧日志不提交。

已新增冻结真值篡改、训练来源混入拒绝测试。随后进行实际共享 Runtime 负载、全套回归；正式启用前仍需用户新一轮杯口到嘴/靠脸反例与摄像头联合验证，不再利用这段留出预测调整 r03。
